<UserControl x:Class="WeddingFinancePlanner.Views.PaymentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:WeddingFinancePlanner.Converters"
             Background="{StaticResource BackgroundPrimaryBrush}">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolVis"/>
        <conv:PaymentStatusToColorConverter x:Key="StatusColor"/>
        <conv:PaymentStatusToBackgroundConverter x:Key="StatusBg"/>
        <conv:DateFormatConverter x:Key="DateFmt"/>
    </UserControl.Resources>
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Payments" Style="{StaticResource H1Text}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Command="{Binding AddPaymentCommand}">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{StaticResource IconAdd}" Width="14" Height="14" Margin="0,0,6,0" Fill="White" Stretch="Uniform"/>
                    <TextBlock Text="Add Payment"/>
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,16">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Tag="Search payments..."/>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <DataGrid ItemsSource="{Binding FilteredPayments}" AutoGenerateColumns="False"
                      IsReadOnly="True" SelectionMode="Single"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Vendor" Binding="{Binding Vendor.Name}" Width="140"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat='${0:N2}'}" Width="100"/>
                    <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat='{}{0:MMM d, yyyy}'}" Width="120"/>
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Status, Converter={StaticResource StatusBg}}"
                                        CornerRadius="6" Padding="6,3" HorizontalAlignment="Left" Margin="0,4">
                                    <TextBlock Text="{Binding Status}" FontSize="11"
                                               Foreground="{Binding Status, Converter={StaticResource StatusColor}}"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Payment Method" Binding="{Binding PaymentMethod.Name}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>

        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{Binding FilteredPayments.Count, Converter={StaticResource BoolVis}, ConverterParameter=invert}">
            <Path Data="{StaticResource IconPayments}" Width="48" Height="48" Fill="{StaticResource BorderBrush}" Stretch="Uniform" HorizontalAlignment="Center"/>
            <TextBlock Text="No payments yet" Style="{StaticResource H2Text}" HorizontalAlignment="Center" Margin="0,16,0,8"/>
            <TextBlock Text="Add your first payment to start tracking" Style="{StaticResource BodySecondaryText}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
