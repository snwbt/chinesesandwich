<UserControl x:Class="WeddingFinancePlanner.Views.ItemListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:WeddingFinancePlanner.Converters"
             Background="{StaticResource BackgroundPrimaryBrush}">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolVis"/>
        <conv:CurrencyConverter x:Key="Currency"/>
        <conv:DateFormatConverter x:Key="DateFmt"/>
    </UserControl.Resources>
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Items to Buy" Style="{StaticResource H1Text}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Command="{Binding AddItemCommand}">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{StaticResource IconAdd}" Width="14" Height="14" Margin="0,0,6,0" Fill="White" Stretch="Uniform"/>
                    <TextBlock Text="Add Item"/>
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,16">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Tag="Search items..."/>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <DataGrid ItemsSource="{Binding FilteredItems}" AutoGenerateColumns="False"
                      IsReadOnly="True" SelectionMode="Single"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Vendor" Binding="{Binding Vendor.Name}" Width="150"/>
                    <DataGridTextColumn Header="Estimated" Binding="{Binding EstimatedCost, StringFormat='${0:N2}'}" Width="110"/>
                    <DataGridTextColumn Header="Actual" Binding="{Binding ActualCost, StringFormat='${0:N2}'}" Width="110"/>
                    <DataGridTextColumn Header="Deadline" Binding="{Binding Deadline, StringFormat='{}{0:MMM d, yyyy}'}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>

        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{Binding FilteredItems.Count, Converter={StaticResource BoolVis}, ConverterParameter=invert}">
            <Path Data="{StaticResource IconItems}" Width="48" Height="48" Fill="{StaticResource BorderBrush}" Stretch="Uniform" HorizontalAlignment="Center"/>
            <TextBlock Text="No items yet" Style="{StaticResource H2Text}" HorizontalAlignment="Center" Margin="0,16,0,8"/>
            <TextBlock Text="Add items you need to purchase for your wedding" Style="{StaticResource BodySecondaryText}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
